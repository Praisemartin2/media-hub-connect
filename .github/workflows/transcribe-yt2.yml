name: Transcribe YT2 clips
on:
  push:
    branches: [claude/ekabo-video-editing-styles-wrs455]
    paths: ['.github/workflows/transcribe-yt2.yml','.github/scripts/transcribe_va.py']
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install -q faster-whisper && python3 .github/scripts/transcribe_va.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add va/clip_va_tr.json
          git commit -m "Add VA clip transcript [skip ci]" || echo nothing
          git push
